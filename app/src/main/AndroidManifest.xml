<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.articulos_main">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MainActivity">

        <activity android:name=".BorrarProducto"></activity>
        <activity android:name=".BorrarTabla" />
        <activity android:name=".Modificar" />
        <activity android:name=".ConsultaPorNombre" />
        <activity android:name=".ConsultaPorId" />
        <activity android:name=".DetalleProducto" />
        <activity android:name=".CrearBaseDatos" />
        <activity android:name=".ConsultaProducto" />
        <activity android:name=".AltaProducto" />
        <activity android:name=".GestorProductos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>